services:
    web:
        build:
            context: .
        command: make web
        ports:
            - "4040:4040"
        environment:
            - HF_TOKEN=${HF_TOKEN}  # Authenticate HF connection via local env var
        models:
            - llm

models:
    llm:
        model: ai/gemma3-vllm:270M
